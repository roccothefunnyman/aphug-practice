# CLAUDE.md

Context for Claude Code when working in this repo.

## What this project is

A static quiz app for **AP Human Geography** practice. 60 multiple-choice questions, generated by Claude on claude.ai for Rocco's daughter to use studying for the AP exam. The app is plain HTML / CSS / vanilla JS.

The repo is published as a public GitHub Pages site so she can study from any device.

## Owner

Rocco Cuffari. Built this for his daughter.

## Project structure

```
aphug-practice/
├── build.py                        # parses source/*.md → app/questions.json
├── README.md                       # user-facing docs
├── CLAUDE.md                       # this file
├── .gitignore
├── source/
│   ├── AP_HuG_Practice_Questions.md  # 60 questions, grouped by unit
│   └── AP_HuG_Practice_Answers.md    # answer key with explanations
└── app/
    ├── index.html                  # references style.css?v=N and app.js?v=N
    ├── style.css
    ├── app.js
    ├── voice.js                    # TTS + ASR singleton (browser-native)
    └── questions.json              # generated, committed
```

60 questions total, all multiple-choice (A-D). No case studies. No FRQs.

## AP HuG units

The College Board CED groups AP Human Geography into 7 units. Per-unit progress bars and picker chips are driven by these:

1. Thinking Geographically
2. Population & Migration
3. Cultural Patterns & Processes
4. Political Patterns & Processes
5. Agriculture & Rural Land Use
6. Cities & Urban Land Use
7. Industrial & Economic Development

Current bank distribution (`py build.py` prints this on every build): U1=15, U2=6, U3=7, U4=8, U5=10, U6=5, U7=9.

## Source file format

Questions and answers live in two single markdown files (not per-question files like the AB-100 sibling project).

**`source/AP_HuG_Practice_Questions.md`** — split by `## Unit N: <Name>` and `### Question N` headers. Each question body is the stem (may include markdown tables for data stimuli) followed by `A) ... B) ... C) ... D) ...` options. Tables and other markdown render through marked.js inside the question card.

**`source/AP_HuG_Practice_Answers.md`** — each answer block starts with `**QN: <letter> — <headline>**`, then 1-3 paragraphs of explanation, then a footer line `*Topic X.Y | Skill N.A*`. The footer maps to the College Board CED topic and skill codes.

## Build

```
py build.py
```

Walks `source/`, parses both files, writes `app/questions.json`. The build script:
- Splits questions on `### Question N` and tracks the most recent `## Unit N` header to assign each question its unit.
- Strips `*End of practice test.*` and trailing `---` rules.
- Pulls out the option letters and their text.
- Pairs each question with its answer block by question number.
- Extracts the topic code and skill code from the answer footer.
- Self-contained, no Python dependencies beyond the stdlib.
- Prints the per-unit count and any unmatched question numbers.

## Run locally

```
cd app
py -m http.server 8000
```

Then http://localhost:8000. Opening `index.html` directly via `file://` will fail because the page uses `fetch()` to load `questions.json`.

## Deploy

GitHub Pages, public repo, branch `main` (or `master`), root folder. URL: `https://<user>.github.io/aphug-practice/app/`.

### Cache busting

`index.html` loads `style.css?v=N`, `voice.js?v=N`, and `app.js?v=N`. **Bump N whenever JS or CSS changes in a way that depends on new markup IDs.** Otherwise browsers may load the new HTML against cached old JS and silently break. Bump in lockstep across all three.

## App features

- **Picker modal.** Topbar `New session` opens it. Quick-pick chips: All / None / Wrong only / Unanswered / Correct only / Unit 1-7. Per-unit All/None links. Per-question checkboxes with one-line stem preview and mastery dot.
- **Progress bars driven by the active subset.** Top bar `correct / wrong / total` plus per-unit bars below it (7 colored bars, one per AP HuG unit).
- **Mastery persistence.** Per-question correct/wrong stored in `localStorage.aphug_stats_v1`, keyed by question id (`q01` ... `q60`). `Reset progress` clears it.
- **Light / dark theme.** Toggle in the topbar; persisted to `localStorage.aphug_theme`.
- **Voice.** Read-aloud (TTS), push-to-talk spoken answers (ASR), and a voice-only mode that auto-reads each question and listens for an answer. All browser-native via `voice.js`. No API key.
- **Slide controls.** `Back` (`←`), `Clear` (wipe current pick), `Copy Question` (stem + options to clipboard). Multiple-choice auto-reveals on click; no Reveal button needed.
- **Session state.** `sessionStorage.aphug_session_v1` holds `{ order, cursor }` so refresh resumes; closing the tab loses it. Mastery and theme live in `localStorage` and survive across tabs.
- **First launch** auto-starts a session with all 60 questions. The picker is only shown when the user clicks `New session` or `Start New Session`.

## When adding new questions

1. Append the new `### Question N` block to the right `## Unit N` section of `source/AP_HuG_Practice_Questions.md`. Match the existing shape: stem, blank line, A-D options.
2. Add the matching `**QN: <letter> — <headline>**` block to `source/AP_HuG_Practice_Answers.md` under the same unit. Include a `*Topic X.Y | Skill N.A*` footer.
3. Run `py build.py` to refresh `app/questions.json`.
4. Commit and push. If you also touched `app.js`, `voice.js`, or `style.css`, bump the `?v=N` cache-busters in `index.html`.

## Validating new content

These questions were AI-generated, so accuracy is a known concern. Before pushing new questions:
- Spawn parallel sub-agents, ~20 questions each, to validate every stated correct answer against the College Board CED and standard textbooks (Rubenstein, AMSCO, Barron's).
- Each agent should flag wrong keyed answers, ambiguous distractors, factual errors in explanations, and CED-misaligned wording.
- Surface flagged items to Rocco for approval before editing the source files.

The first 60 were validated this way at repo creation; results: 57 fully OK, 3 minor stylistic notes (Q7 distractor ambiguity, Q20 explanation wording, Q48 explanation wording).

## Local dev tips

- The repo is OneDrive-synced. Avoid creating massive build artifacts.
- `questions.json` is committed (it is a generated artifact, but committing it means GitHub Pages can serve the app without a build step).

## Sibling project

`../practicetest/` is a similar app for Microsoft's AB-100 (Agentic AI Business Solutions Architect) certification. Same skeleton, different content. Do not touch that folder when working here.
